# Puerto del servidor (default 3000)
PORT=3000

# URL pública del servidor (para compartir QR en producción)
# En desarrollo se calcula desde host:port automáticamente.
# NEXT_PUBLIC_PUBLIC_URL=https://mi-juego.onrender.com
